<h1> Index of all conference attendees </h1>

<% @attendees.each do |attendee| %>
    <div>
      <%= attendee.name %>
      <%= attendee.company %>
      <%= attendee.email %>
      <%= attendee.registered %>

    </div>
  <br>
<% end %>
